1. ENHANCEMENT - add a version number and information about where the container is running to the bottom left to make it easier to distinguish which version is running where (i.e. prod vs local). This could be the docker version tag, the deployment build or sha or commit id or something else. suggest the best way to track and display the version to simplify development.

2. MAJOR FEATURE - I'd like to add WhatsApp integration.  Create a new tab in the Settings screen for WhatsApp integration to provide the Meta Cloud API Key.  The Users tab should have a Phone Number field so users can be identified when they use WhatsApp to send a message and chat history will be assigned to that user.  When a user who is not registered for the platform sends a message, the bot should respond with a link to the web interface to sign in with Plex.  New users must provide a phone number (optional) if they wish to converse via WhatsApp.  Users can change their phone number in Settings.  Sessions created via WhatsApp should have a 24 hour limit.  The bot should respond when it is @ messaged "@Thinkarr" and replies should be sent as quoted replies.  WhatsApp messages will also need to handle the card/carousel concept for titles (send the thumbnail as images, and use interactive buttons for request / watch).  As the WhatsApp bot may exist in Groups, one person may request a title but another person may click the button to request the title and it may be requested multiple times (the tool call should handle this gracefully and explain the title has already been requested.)

3. BUG - The title carousel does not reliably display left and right navigation arrows, making it non-obvious to users that they can scroll through results. The arrows appear inconsistently — sometimes shown, sometimes not — so users may not realise the carousel is scrollable.

4. IMPROVEMENT - The system prompt is user-editable, but app updates may ship an improved default system prompt. Separate the concept of a default system prompt (shipped with the app) and a custom system prompt (user-edited). Add a "Reset to Default" button in Settings that restores the custom system prompt to the current default, so users can easily adopt improvements without losing the ability to customise.

5. ENHANCEMENT - logs should use the timestamp from the TZ variable set by docker compose.